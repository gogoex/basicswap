# proposed changes
Always show what will be changed with line numbers for confirmaiton instead of asking whether to commit the changes.

# branch
All the changes should be applied to `add-navio` branch instead of `main`

# nav.py
- functions should be sorted alphabetically by function name
